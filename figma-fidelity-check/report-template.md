# 还原度验证报告

- **页面/Frame**：<页面名 + Figma node-id>
- **目标尺寸 / DPR**：<如 375×812 / 3>
- **实现来源**：<URL 或小程序截图>
- **渲染配置**：<浏览器、字体加载、滚动条、截图范围>
- **日期**：<YYYY-MM-DD>

## 验收结论

- 🔴：N 项
- 🟡：N 项（均已执行修复并复验：是 / 否）
- 最大间距偏差：Npx（要求 ≤2px）
- 颜色：全部命中 Figma token / 存在偏差（无 token 时列精确颜色值）
- 环境噪声基线：已建立 / 未建立
- **是否达标**：是 / 否

> 达标条件：🔴=0；🟡≤2 且已执行修复并复验；所有间距偏差≤2px；颜色命中 token 或精确设计色值。

## Figma 约束取数记录

| 容器/节点 | 节点 ID | 读取值 | 直接子节点已读取 | 备注 |
| --- | --- | --- | --- | --- |
|  |  | layout / gap / padding / 对齐 / 尺寸模式 | 是 / 否 | spacer、绝对定位或嵌套容器 |

## 差异清单

| # | 严重度 | 类别 | 位置 | 实现现状 | Figma 实际值 | 修改位置/建议 | 复验 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| 1 | 🔴 | 间距 | 顶部标题 | 24px | 父容器 `padding-top: 16px` | 改为 16px | 未复验 |

## 环境基线命中（不计红黄项）

| 类别 | 表现 | 基线证据 | 本轮配置一致性 |
| --- | --- | --- | --- |
|  |  | 已验收页面的对照截图/记录 | 视口、DPR、字体、浏览器均一致 |

没有已验收基线时填写“未建立”，不得在此列出豁免项。

## 截图对照

- 基准（Figma）：<路径/引用>
- 实现（渲染）：<路径/引用>
- 基线（如有）：<已验收页面的两张对照图/记录>
